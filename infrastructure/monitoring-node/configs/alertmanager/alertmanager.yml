global:
  resolve_timeout: 5m

route:
  receiver: default-receiver
  group_by:
    - alertname
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: default-receiver

# Example webhook receiver for later use. Do not commit real webhook secrets.
# receivers:
#   - name: discord
#     discord_configs:
#       - webhook_url: https://discord.com/api/webhooks/replace-me/replace-me
#         send_resolved: true
